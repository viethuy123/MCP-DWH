# Database
DB_HOST=postgres        # ← tên service docker, không phải localhost
DB_PORT=5432
DB_NAME=your_db
DB_USER=your_user
DB_PASSWORD=your_password

# MCP Server
MCP_HOST=0.0.0.0
MCP_PORT=8000

# dbt
DBT_MANIFEST_PATH=dbt/manifest.jsons